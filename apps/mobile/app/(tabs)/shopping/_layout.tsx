import { Stack } from 'expo-router';

// Shopping tab stack. Phase 3 fills in the shopping list (W03) content.
export default function ShoppingStackLayout(): React.JSX.Element {
  return <Stack screenOptions={{ headerShown: false }} />;
}
