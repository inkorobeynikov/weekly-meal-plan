# Changelog

- 2026-06-04 F3 recipe placeholders — replace gray/blurhash placeholders with deterministic colored+glyphed RecipePlaceholder cards (packages/ui-native); upgrade recipe detail hero + Pressable tabs + hide !canSwap CTA; pass placeholderSeed into MealCard from plan/index
- 2026-05-23 Single-meal swap now sees the rest of the plan — replaceMeal passes the other meals' (date, mealType, title) to the AI so it doesn't echo an adjacent day's dish (e.g. user replaces Tuesday lunch and the model picks salmon again because Monday lunch was salmon)
- 2026-05-23 Lunch + dinner — AI now generates BOTH obiad (lunch) and kolacja (dinner) per day. New mealType 'lunch' in DB enum (migration 0001), schema and prompt updated; weekday cookingTime is now the SUM cap for both meals; lunch_leftover still optional in place of fresh lunch. Web /plan card shows meal-type label.
- 2026-05-23 Fix stale leftovers on swap — when a dinner is replaced, its lunch_leftover on later days is re-pointed at the new recipe (if isGoodForLeftovers) or deleted; swapping a lunch_leftover itself clears leftoversPlanned
- 2026-05-23 Plan editing UX — swap one meal with a reason text, "dislike + replace" combo (records dont_repeat feedback then regenerates), multi-select mode with shared AI context and bulk-replace endpoint
- 2026-05-23 Plan window now starts TOMORROW (not today), so today's shopping run covers a plan that begins tomorrow and ends on the Sunday after the upcoming one
- 2026-05-23 /reset command + variable plan window — clear active plan/shopping list and regenerate from today through the Sunday after the upcoming one (8–14 days)
- 2026-05-20 Database bootstrap — generated initial Drizzle migration and applied schema to the development DB
- 2026-05-20 Local dev stability — load root .env for bot dev and resolve workspace TS sources in web dev
- 2026-05-20 Dev startup stability — transpile @meal-planner/bot in Next and lazy-init OpenAI client
- 2026-05-19 Phase 3: AI plan generation — generateWeeklyPlan, replaceMeal, Inngest job
- 2026-05-19 Phase 4a: Telegram initData auth + JWT middleware
- 2026-05-19 Phase 4b: W04 Plan Review + W01 Weekly Plan screens
- 2026-05-19 Phase 4c: W02 Recipe Detail + W03 Shopping + W05 Family screens
- 2026-05-19 Phase 5: full bot onboarding flow, /shopping command, webhook handler
- 2026-05-19 Phase 6: shopping list generation with ingredient aggregation and categorization
- 2026-05-20 Phase 7: feedback loop — family memory, reaction UI, daily bot reminder
- 2026-05-20 Phase 8: promotions matching + shopping list promo hints
- 2026-05-20 Phase 9: analytics instrumentation, week-2 retention trigger, CI + deployment config
- 2026-05-20 Fix: plan-generate now notifies the household on Telegram with a summary + approve button; added bot approve callback (mirrors web, triggers shopping.generate)
- 2026-05-20 Rolling weekly plan: generate on any weekday (weekStartDate = today, Europe/Warsaw); AI prompt now derives weekday/weekend from a per-day Day map instead of fixed Mon-Fri offsets; /plan surfaces draft + approve button and offers generation when empty
- 2026-05-20 Plan starts next day (rolling Thu→Wed etc.); wired Telegram Mini App auth on web (telegram-web-app.js + AuthGate → /api/auth/telegram); bot now opens the plan via web_app buttons + chat menu button; added cloudflared dev:tunnel automation + allowedDevOrigins
- 2026-05-20 dev:tunnel now also launches the Inngest Dev Server (-u /api/inngest) and runs turbo in stream mode — full local stack (tunnel + web + bot + Inngest) from one command
- 2026-06-02 Add apps/mobile React Native app (Expo Router, NativeWind, BetterAuth) + packages/ui-native design system; additive BetterAuth path in apps/web (no global middleware)
- 2026-06-03 Mobile E2E suite (apps/mobile/e2e): Maestro flows for all 9 screens driving the real app against a typed, dependency-free mock API server with switchable scenarios (no-plan / draft / approved / plan-with-allergen / shopping-active / shopping-all-bought); same flows run on iOS Simulator (macOS) + Android emulator (Windows) with documented localhost vs 10.0.2.2 host networking; added stable testIDs across screens + Button/MealCard (jest still 21+41 green, typecheck green); opt-in nightly GH Actions (non-gating); proves allergy HARD-CONSTRAINT keeps approve disabled
- 2026-06-03 "Plately" visual redesign: re-skinned design tokens (web + native mirror) to forest-green + lime on a clean near-white green-tinted background (dropped warm beige), minimal hairline cards, SF Pro system typography, forest-green primary CTA + lime tab accent; retuned food-tile placeholders and removed leftover hardcoded beige. Typecheck green across 9 packages; ui-native + mobile tests pass
- 2026-06-03 Wire mobile app to existing backend routes: replaceMeal + updateShoppingItem now unwrap their real routes ({ meal } / { item }); added POST /api/shopping/lists/:listId/items (withAuth + Zod → shoppingService.addManualItem, normalization/categorization via exported domain helpers). Reworked Recipe Swap (W07) to A4 Variant 1 "reason → replace" flow (dropped the unbuilt /alternatives endpoint). Typecheck green across 9 packages; mobile Jest 42 tests pass
- 2026-06-03 Push notifications replace the (now dormant) Telegram bot channel: new `push_tokens` table + migration 0003; `notificationService.notifyHousehold()` in packages/domain sends via expo-server-sdk and prunes DeviceNotRegistered tokens (no next/grammy imports); `POST /api/push/register` (withAuth + Zod upsert); mobile requests permission, registers its Expo token on authenticated app-start, and routes notification taps (plan→plan tab, feedback→feedback). Rewired plan-generate + retention-trigger to push and moved the 18:00 Europe/Warsaw feedback reminder to a web Inngest cron; getWeekTwoRetentionCandidates now requires ≥1 push token. Removed the Telegram webhook route and dropped @meal-planner/bot + grammy from apps/web. Typecheck green across 9 packages; tests pass
- 2026-06-04 Fix weekly loop + async generation (F1): new `usePlanGeneration` hook centralises generate→poll (8s interval, ~15-poll/2-min ceiling, then explicit error + "Spróbuj ponownie"; never polls forever) and is reused by the plan, review and shopping screens. W04 "Wygeneruj nowy" now waits for a genuinely new draft (different id) instead of refetching the stale one, and shows a "Generuję nowy plan…" state; after approve it shows a confirmation with a link to the Zakupy tab. W08 "Rozpocznij nowy tydzień" now routes to weekly feedback (W09) first, then generates with the poll pattern and surfaces errors (no silent catch). W09 made reachable in-app (end-of-week CTA on the approved plan + the new-week flow), guards submitFeedback when planId is null, and adds a "Wróć do planu" button on the success state. W01 day cards gained a swap affordance (RecipeSwapSheet) + feedback entry. W03 shopping: checkbox toggles both ways, grouped list wrapped in a ScrollView, manual items get a category picker + per-row delete backed by a new DELETE /api/shopping/items/:itemId route (thin + Zod + withAuth → shoppingService.deleteItem). Typecheck green across 9 packages; mobile Jest 13 suites / 44 tests pass
- 2026-06-04 Intelligent surface (F4): added Zod-validated LLM fields RecipeSchema.isTryNew + priceEstimateGrosze (grosze, integer) in packages/ai and additive migration 0004 (recipes.is_try_new/price_estimate_grosze, planned_meals.badges_json/cooked_at, shopping_list_items.estimated_price_grosze — all nullable). Domain derives per-dish badges (kid_ok/leftovers/try_new) via shared deriveMealBadges and persists them on each planned meal at generation + swap; new markMealCooked method + POST /api/plans/:planId/meals/cooked (withAuth + Zod + ownership). Mobile: "why this plan" reasoning block on W01/W04, per-dish badges on day cards + recipe detail, W02 collapsible storage/for-kids/swaps sections + "mark cooked", W07 quick reason chips + "Zamieniam…" state, W03 shopping lines now show source recipe + "potrzebne do <dzień>" + promo (Biedronka/Lidl) + a real grosze cost estimate (footer total), W04 Approve-primary/Regenerate-ghost, and fixed the kids_didnt_eat feedback step (was mislabelled "Średnio" → "Dzieci nie zjadły"). The allergy/hardRestrictions retry loop is untouched and still runs before any badge/price processing. Typecheck green across 9 packages; mobile Jest 14 suites/64 tests + ui-native 25 tests pass
